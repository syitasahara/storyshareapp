{
  "name": "storyshareapp",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && npm run deploy:netlify"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vite-plugin-pwa": "^0.20.0"
  }
}
